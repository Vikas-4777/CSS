spring.application.name=course-selection-system

# Server Port (Fall back to 8080 if PORT env var is not set)
server.port=${PORT:8080}

# MySQL datasource (Fallback to local if no cloud variables)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/coursedb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Vikas4777}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Admin credentials
admin.email=admin@gmail.com
admin.password=Admin@123

# JWT Configuration
jwt.secret=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
jwt.expiration=86400000
